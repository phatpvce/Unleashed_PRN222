@model IEnumerable<Unleashed_MVC.Models.SaleStatus>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SaleStatusName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SaleStatusName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SaleStatusId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SaleStatusId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SaleStatusId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
